# Команды для настройки Railway переменных
# Выполните эти команды после того, как Railway CLI будет доступен

# Основные переменные
railway variables set TELEGRAM_BOT_TOKEN=8074208505:AAEvJ0vqw6TVGs06gOWqxf2h8B1MjWnnBWI
railway variables set TELEGRAM_ADMINS=nikiname
railway variables set REDIS_DB=0
railway variables set TRIGGER_WORDS=жопа,мат

# Redis переменные (получите из Redis сервиса в Railway Dashboard)
# railway variables set REDIS_HOST=<значение_из_redis>
# railway variables set REDIS_PORT=<значение_из_redis>
# railway variables set REDIS_PASSWORD=<значение_из_redis>

# Или используйте скрипт:
# ./setup-railway-vars.sh
